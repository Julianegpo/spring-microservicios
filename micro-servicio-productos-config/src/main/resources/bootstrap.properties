# aqui se añade solo las propiedades de Spring Cloud Config

# esto sobreescribe el puerto configurado en el propio properties del microservicio
# prevalece bootstrap.properties sobre application.properties
spring.application.name=servicio-productos
# url del servidor de config
spring.cloud.config.uri=http://localhost:8100/